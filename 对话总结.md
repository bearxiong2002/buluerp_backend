# 对话总结

## 2026-05-31

- 当前任务：新增“导入模具时一并创建模具厂商”的后端接口，不修改数据库结构。
- 背景结论：原逻辑要求先维护 `erp_manufacturer` 厂商，再导入/新增模具并写入 `erp_mould.manufacturer_id`；新需求是在导入模具 Excel 时，如果厂商名称不存在，则自动创建厂商后继续创建模具。
- 本次实现：
  - 新增接口 `POST /system/mould/import/with-manufacturer`。
  - 复用原 `AddMouldRequest` 导入模板。
  - 新增 `IErpMouldService.addWithManufacturer(AddMouldRequest request)`。
  - 新增导入逻辑：厂商名称为空时报错；按厂商名称精确查询 `erp_manufacturer`；不存在则创建厂家；随后设置模具 `manufacturerId`、默认状态 `新模(排产中)`、默认 `mouldHouseId=-1` 并插入模具。
- 验证：已执行 `mvn -pl ruoyi-admin -am -DskipTests clean compile`，构建成功。

## 2026-06-02

- 当前任务：修改本地开发配置连接测试数据库，并将后端服务部署到远程服务器。
- 配置变更：
  - `application-dev.yml` 的 MySQL 主库改为远程测试库 `101.200.186.223:3306/buluerp`，使用 `buluerp` 业务用户。
  - Redis 配置按部署后本机访问设置为 `localhost:26379`。
- 数据迁移：
  - 本地 `root/3306` 的 `buluerp` 导出为 `output/buluerp-local-data-notablespaces-20260602114505.sql`。
  - 去除 dump 中 `DEFINER` 后导入远程 MySQL；远程导入后校验为 67 张表，`sys_user` 17 条，`sys_menu` 245 条。
  - 远程导入前备份保存在 `/opt/buluerp/backups/`。
- 部署：
  - 后端 jar 部署到 `/opt/buluerp/backend/ruoyi-admin.jar`。
  - systemd 服务名为 `buluerp-backend`，端口 `8080`。
  - 远程 Redis 已确认 `0.0.0.0:26379` 监听，密码验证 `PING` 返回 `PONG`，版本 Redis 8.0.5。
- Swagger 修复：
  - `SwaggerConfig` 的 Docket 文档类型改为 `DocumentationType.SWAGGER_2`。
  - 批量补齐缺失 `paramType` 的 `@ApiImplicitParam`，修复 `/v2/api-docs` 的 `RequestParameter.getIn()` 空指针错误。
- 验证：
  - 已执行 `mvn -pl ruoyi-admin -am -DskipTests clean package`，构建成功，并确认 jar 内包含 `BOOT-INF/classes/mybatis/mybatis-config.xml`。
  - 远程 `/v2/api-docs` 返回 HTTP 200，大小约 379 KB；`/doc.html` 返回 HTML 页面。

## 2026-06-03

- 当前任务：新服务器 `8.130.123.199` 清理原手动运行环境，并改用宝塔面板重新安装项目所需环境。
- 宝塔面板：
  - 已安装宝塔 Linux 面板，面板端口改为 `8888`。
  - 访问地址为 `https://8.130.123.199:8888/c772da04`。
- 环境清理：
  - 已停止残留宝塔安装进程。
  - 已清理手动安装的 MySQL、Redis、JDK 以及宝塔半安装目录。
- 宝塔环境安装与配置：
  - 通过宝塔极速安装 MySQL 8.0.24，监听 `*:3306`。
  - MySQL root 密码调整为 `060202`，已创建 `buluerp` 数据库和 `buluerp` 业务用户。
  - 通过宝塔安装 Redis 7.2.12，配置为仅本机监听 `127.0.0.1:26379`，密码 `060202`，无密码访问返回 `NOAUTH`，带密码 `PING` 返回 `PONG`。
  - 通过宝塔 JDK 脚本安装 JDK `17.0.8`，路径为 `/www/server/java/jdk-17.0.8`。
- 防火墙：
  - 当前放行端口包括 `3306/tcp`、`8080/tcp`、`8888/tcp`。
  - 已移除 Redis 外网端口 `26379/tcp` 和宝塔旧随机端口 `28538/tcp`。

### 宝塔面板访问排查

- 用户反馈 `https://8.130.123.199:8888/c772da04` 无法直接访问。
- 排查结论：
  - 服务器本机宝塔进程正常，`8888` 曾监听在 `0.0.0.0`。
  - 宝塔 SSL 关闭后，默认入口改为 `http://8.130.123.199:8888/login`。
  - 对 `8888` 做服务器抓包时，本地访问没有到达服务器网卡，说明问题不在宝塔进程或 firewalld，更像云安全组/上游链路未放行 `8888`。
- 临时处理：
  - 按用户要求改用 `88` 端口测试。
  - 宝塔面板端口已改为 `88`，firewalld 已放行 `88/tcp`。
  - 本地访问 `http://8.130.123.199:88/login` 返回 HTTP 200，页面标题为“宝塔Linux面板”。

### 新服务器后端部署

- 当前任务：将本地后端项目修改为新服务器数据库/Redis 配置后部署到 `8.130.123.199`。
- 本地配置：
  - `application-dev.yml` 主库改为 `jdbc:mysql://127.0.0.1:3306/buluerp?...useSSL=false&allowPublicKeyRetrieval=true...`。
  - MySQL 用户为 `buluerp`，密码为 `1pReYZavtS5tZvyRfFxD`。
  - Redis 改为部署后本机访问 `127.0.0.1:26379`。
  - Redis 密码 `060202` 已加引号写为字符串，避免 YAML 将前导 0 数字化导致应用连接 Redis `WRONGPASS`。
- 数据迁移：
  - 已从本地 `root/060202`、端口 `3306` 的 `buluerp` 导出并上传到服务器。
  - 已导入服务器宝塔 MySQL `buluerp` 库；校验为 67 张表，`sys_user=17`，`sys_menu=245`。
- 部署结果：
  - 新 jar 已上传到 `/opt/buluerp/backend/ruoyi-admin.jar`。
  - systemd 服务 `buluerp-backend` 已启动并保持 `active`。
  - 后端监听 `*:8080`，日志显示 `Started RuoYiApplication in 18.81 seconds`。
  - Redis `127.0.0.1:26379` 使用密码 `060202` 返回 `PONG`。
- Swagger 验证：
  - 服务器本机访问 `http://127.0.0.1:8080/doc.html` 返回 Knife4j HTML 页面。
  - 服务器本机访问 `http://127.0.0.1:8080/v2/api-docs` 返回 Swagger 2.0 JSON。
  - 当前工作站直连公网 `http://8.130.123.199:8080/doc.html` 时 TCP 可连接但 HTTP 返回为空响应；服务器本机访问正常，后续若公网浏览仍异常，优先检查云安全组/上游入口策略。
